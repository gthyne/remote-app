<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.test.myremote1">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRemote1"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- remote_button Activity -->
        <activity
            android:name=".remote_button"
            android:exported="true" />

        <!-- wifi_conection Activity -->
        <activity
            android:name=".wifi_conection"
            android:exported="true" />

        // ActivityWifiConnection -->
        <activity
            android:name=".activity_wifi_conection"
            android:exported="true" />
        //last page buttons
        <activity
            android:name=".activity_remote_button" />




    </application>

</manifest>
